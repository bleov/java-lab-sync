package org.code.theater;

public class Theater {
	/**
	 * Plays a 1D array of {@link Scene} objects, generating an animation and
	 * presenting it in Java Lab. There can be only one call to {@code play()} or
	 * {@code playScenes()} per execution of a program.
	 * 
	 * @param scene the array of {@link Scene} objects to play
	 */
	public static void play(Scene[] scene) {
	}

	/**
	 * Plays the {@link Scene} object provided, generating an animation and
	 * displaying it in Java Lab. There can be only one call to {@code play()} or
	 * {@code playScenes()} per execution of a program.
	 * 
	 * @param scene the {@link Scene} object to play
	 */
	public static void playScenes(Scene scene) {
	}

	/**
	 * Plays all {@link Scene} objects provided as parameters to this call. This
	 * method uses a special feature of Java that allows for any number of
	 * {@link Scene} objects to be passed to the method without requiring an array
	 * to be created. There can be only one call to {@code play()} or
	 * {@code playScenes()} per execution of a program.
	 * 
	 * @param scenes
	 */
	public static void playScenes(Scene... scenes) {

	}
}
